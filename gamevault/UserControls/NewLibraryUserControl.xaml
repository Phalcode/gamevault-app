<UserControl x:Class="gamevault.UserControls.NewLibraryUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:gamevault.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Border Background="{StaticResource DarkBackground4}" Grid.Row="0">
            <TextBlock Text="New Library" FontSize="35" Margin="20,0,0,0" Foreground="White"/>
        </Border>
        <StackPanel Grid.Row="1">
            <Viewbox>
                <local:NewInstallUserControl Width="2000"/>
            </Viewbox>
            <Border Background="{StaticResource MahApps.Brushes.Accent}" Height="50" Margin="10" CornerRadius="5">
                <TextBlock Text="Server Library (420)" FontSize="20" FontWeight="Bold" Margin="40,8,0,0"/>
            </Border>
        </StackPanel>
    </Grid>
</UserControl>
